
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  active: {
    borderWidth: 5,
    borderRadius: 200,
    borderColor: "grey",
  },
  inactive: {
    borderWidth: 3,
    borderRadius: 200,
    borderColor: "white",
  },
});
export default styles;